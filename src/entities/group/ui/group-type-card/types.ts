type GroupTypeCardProps = {
  icon: string;
  title: string;
  description: string;
  iconBg?: string;
  isDashed?: boolean;
  isSelected?: boolean;
  onClick?: () => void;
  className?: string;
};

export type { GroupTypeCardProps };
